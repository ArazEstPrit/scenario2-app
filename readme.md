# Scenario 2 App

## Usage

Make sure you have NPM and Node.js installed, and your Node version is
`v22.18.0` or later. You can check by running `node -v`.

Clone this repo, run `npm i` to install the dependences, and start the app using
`npm start`.
